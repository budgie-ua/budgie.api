defmodule Core.Azure.Services.AnalyzeResultTest do
  @moduledoc false

  use Core.DataCase, async: true

  # alias Core.Azure.Services.AnalyzeResult

  describe "when result is succeed" do
    test "returns parsed json" do
    end
  end
end
